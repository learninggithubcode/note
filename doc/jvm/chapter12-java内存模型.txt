## 硬件的效率与一致性

提高硬件的效率：
1、高速缓存，引入：缓存一致性协议。
2、cpu指令重排序。


## Java 内存模型（JMM）

起因：屏蔽各种硬件和操作系统的内存访问差异，以实现让Java程序在各个平台下都能达到一致的内存访问效果。

目标：定义程序中各个变量的访问规则，即在虚拟机中将变量存储到内存和从内存中取出变量这样的底层细节。
        此处的变量：实例字段、静态字段、构成数组对象的元素。

主内存和工作内存

共享变量都存储在主内存中，工作内存中存放共享变量的拷贝

