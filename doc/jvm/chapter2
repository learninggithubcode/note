运行时数据区域

    方法区
    虚拟机栈
    本地方法栈
    堆
    程序计数器

HotSpot虚拟机对象

	对象的创建
		-- jvm视角
		类加载
		分配内存
		初始化为零值  --> 保证了对象的实例字段在java代码中不赋初始值就直接使用
		对象的设置（对象头信息等）
		-- java视角
		init()

	对象的内存布局
		对象头
			对象自身的运行时数据（哈希码、锁状态标志等等）
			类型指针
		实例数据
			对象真正存储的有效信息
		对齐填充
			占位符，保持8字节的整倍数

	对象的访问定位
		句柄（句柄池）
		直接指针